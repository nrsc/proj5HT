% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_asp_pipeline.R
\name{run_asp_pipeline}
\alias{run_asp_pipeline}
\title{Run the per-cell ASP + wash-in pipeline}
\usage{
run_asp_pipeline(
  cell,
  mMD = NULL,
  mMD_path = "~/proj5HT/den/serotonin/Data/Master_UI_data.ods",
  mMD_sheet = "Master_Macaque",
  ttl_default = 5,
  outlier_sd = 5,
  bl_window = c(-5, 0),
  save_srt = TRUE,
  make_concat_plot = TRUE,
  make_single_plots = TRUE,
  make_washin = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{cell}{Character. proj5HT cell identifier (e.g. \code{"QN26.26.025.20.10.01"}).}

\item{mMD}{Optional data frame. Pre-loaded Master_UI sheet rows. If \code{NULL},
\code{mMD_path} + \code{mMD_sheet} are read on demand.}

\item{mMD_path}{Path to the Master_UI ODS file.}

\item{mMD_sheet}{Sheet name within \code{mMD_path}. Defaults to \code{"Master_Macaque"}.}

\item{ttl_default}{Default trial-time-length (seconds) when not specified
in the Master_UI row.}

\item{outlier_sd}{SD threshold for ASP outlier rejection.}

\item{bl_window}{Numeric length-2. Baseline window (s) relative to puff
onset.}

\item{save_srt}{Logical. If \code{TRUE}, persists the child nnest to
\verb{<rookery>/<cell>/<cell>-srt.rds}.}

\item{make_concat_plot}{Logical. Render the concatenated ASP plot.}

\item{make_single_plots}{Logical. Render one plot per ASP protocol.}

\item{make_washin}{Logical. Run \code{\link[=DrugWashIn_from_asp]{DrugWashIn_from_asp()}} and persist the
wash-in CSV.}

\item{verbose}{Logical. Echo progress from downstream calls.}
}
\value{
An invisible list with elements:
\describe{
\item{\code{asp}}{The ASP payload list returned by \code{\link[=ensure_asp]{ensure_asp()}}.}
\item{\code{concat}}{ggplot object (only if \code{make_concat_plot = TRUE}).}
\item{\code{single}}{Named list of ggplots, one per protocol key
(only if \code{make_single_plots = TRUE}).}
\item{\code{washin}}{Wash-in result list (only if \code{make_washin = TRUE}).}
}
}
\description{
One-call wrapper around \code{\link[=ensure_asp]{ensure_asp()}} (build / load the Aligned Spike
Protocol payload) and \code{\link[=DrugWashIn_from_asp]{DrugWashIn_from_asp()}} (derive ket/WAY wash-in
kinetics from the ASP). Optionally produces concatenated and per-protocol
ASP plots.
}
\details{
Driven by \code{dev/startup.sh} (alias \code{update_5ht}) to incrementally analyze
any cell in the \code{Master_UI_data.ods} Master_Macaque sheet that does not yet
have a \verb{<cell>-srt.rds} in the rookery.
}
\examples{
\dontrun{
run_asp_pipeline("QN26.26.025.20.10.01")
}

}
