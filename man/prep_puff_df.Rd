% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify5HT.R
\name{prep_puff_df}
\alias{prep_puff_df}
\title{Prepare a raw puff dataframe for classification}
\usage{
prep_puff_df(
  df,
  baseline_window = c(-5, 0),
  subclass_keep = c("L23_IT", "L5_ET", "L5_IT"),
  recode_subclass = c(L3c = "L23_IT"),
  bath_keep = "none",
  puff_pattern = "^5HT"
)
}
\arguments{
\item{df}{data.frame with at least: \code{cell_name}, \code{assigned_subclass},
\code{time}, \code{x_bin}, \code{instRate}, \code{bath}, \code{puff}.}

\item{baseline_window}{numeric length-2. Time window (seconds) used to
compute baseline mean \code{instRate} per cell.}

\item{subclass_keep}{character vector of subclass labels to retain.}

\item{recode_subclass}{named character vector for merging subclass labels
(e.g. \code{c("L3c" = "L23_IT")}).}

\item{bath_keep}{character scalar — bath condition to keep.}

\item{puff_pattern}{regex pattern applied to the \code{puff} column.}
}
\value{
The filtered / augmented data.frame with added columns:
\code{baseline_hz}, \code{n_baseline_pts}, \code{pct_of_baseline}.
}
\description{
Filters by bath, subclass, and puff drug; computes per-cell baseline
firing rate and \code{pct_of_baseline} (100 = baseline).
}
