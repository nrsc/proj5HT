% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/response_metrics5HT.R
\name{build_response_metrics_table}
\alias{build_response_metrics_table}
\title{Compute response metrics for every cell in a prepared puff dataframe}
\usage{
build_response_metrics_table(
  df_prepped,
  baseline_window = c(-5, 0),
  response_window = c(0, 10),
  cusum_k = 0.5,
  cusum_h = 5,
  wasserstein_grid = 1024
)
}
\arguments{
\item{df_prepped}{data.frame from \code{prep_puff_df()} (long format,
one row per timepoint per cell).}

\item{baseline_window, response_window, cusum_k, cusum_h, wasserstein_grid}{passed through to \code{response_metrics5HT()}.}
}
\value{
tibble with one row per \code{cell_name} and the metric panel.
}
\description{
Groups \code{df_prepped} (output of \code{prep_puff_df()}) by
\code{cell_name} and applies \code{response_metrics5HT()} to each
cell.  The returned per-cell tibble can be left-joined onto the
output of \code{build_per_cell_classifier()} to enrich the existing
classifier with robust-baseline metrics.
}
\examples{
\dontrun{
  out      <- classify_puff_response(comp5HT$srtPuff)
  metrics  <- build_response_metrics_table(out$df_prepped)
  enriched <- dplyr::left_join(out$per_cell, metrics, by = "cell_name")
}
}
