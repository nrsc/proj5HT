% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/proj5HT_loop_helper_functions.R
\name{run_asp_fig_qc_loop}
\alias{run_asp_fig_qc_loop}
\title{Interactive QC loop for ASP figures with adjustable axis settings}
\usage{
run_asp_fig_qc_loop(
  srt,
  make_figs_fn,
  y_mode = "log2_fc",
  y_lim_single = c(NA_real_, NA_real_),
  y_lim_concat = c(NA_real_, NA_real_),
  x_lim_single = NULL,
  x_lim_concat = NULL,
  show_title = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{srt}{A nnest-like object containing \code{srt$dfs$asp} and \code{srt$cell}.}

\item{make_figs_fn}{Function that takes \verb{(srt, y_lim_single, y_lim_concat, y_mode, x_lim_single, x_lim_concat)}
and returns an updated \code{srt} with figures stored (e.g. \code{srt$figs$singles}, \code{srt$figs$concat}, \code{srt$figs$washin}).}

\item{y_mode}{Character scalar. Initial y-mode to pass through to plotting.}

\item{y_lim_single}{Numeric length-2. Initial y-lims for single-protocol plots.}

\item{y_lim_concat}{Numeric length-2. Initial y-lims for concatenated plots.}

\item{x_lim_single}{NULL or numeric length-2. Optional x-lims for single-protocol plots.}

\item{x_lim_concat}{NULL or numeric length-2. Optional x-lims for concatenated plots.}

\item{show_title}{Logical. If TRUE, show current settings in the select.list title.}

\item{verbose}{Logical. If TRUE, prints small messages (e.g. invalid inputs).}
}
\value{
A list with:
\describe{
\item{\code{srt}}{Updated \code{srt} with figures attached.}
\item{\code{call}}{Final decision: "keep" or "omit".}
\item{\code{settings}}{List of final settings used (y_mode, y_lim_single, y_lim_concat, x_lim_single, x_lim_concat).}
}
}
\description{
Repeatedly generates per-protocol and concatenated ASP figures, displays them,
and prompts the user to choose keep/omit/rerun, with an option to adjust axis
settings (y-mode, y-lims, optional x-lims) between reruns.
}
