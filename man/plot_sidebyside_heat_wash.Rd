% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_sidebyside_heat_wash.R
\name{plot_sidebyside_heat_wash}
\alias{plot_sidebyside_heat_wash}
\title{Plot baseline vs wash ASP protocols side-by-side (trace + heat strip)}
\usage{
plot_sidebyside_heat_wash(
  x,
  baseline_key = x$dfs$dwin$baseline_key,
  wash_key = x$dfs$dwin$wash_key,
  heat_dt = 1,
  ttl_shape = 6,
  y_mode = "log2_fc",
  y_lim = c(-2, 1),
  save_png = FALSE,
  show_plot = FALSE
)
}
\arguments{
\item{x}{A nested list (e.g., your nnest/srt-like object) that contains:
\itemize{
\item \code{x$dfs$asp}: ASP object compatible with \code{plot_asp_single_protocol()}
\item \code{x$dfs$dwin$baseline_key}: protocol key for baseline
\item \code{x$dfs$dwin$wash_key}: protocol key for wash
}}

\item{baseline_key}{Character scalar. Baseline protocol key. Default:
\code{x$dfs$dwin$baseline_key}.}

\item{wash_key}{Character scalar. Wash protocol key. Default:
\code{x$dfs$dwin$wash_key}.}

\item{ttl_shape}{Numeric/integer. Shape code passed through to
\code{plot_asp_single_protocol()} for TTL markers. Default \code{6}.}

\item{y_mode}{Character. Y scaling mode passed to \code{plot_asp_single_protocol()}.
Default \code{"log2_fc"}.}

\item{y_lim}{Numeric length-2 vector. Shared y-limits for both panels.
Default \code{c(-2, 1)}.}

\item{save_png}{Logical. Passed to \code{plot_asp_single_protocol()} for both plots.
Default \code{FALSE}.}

\item{show_plot}{Logical. Passed to \code{plot_asp_single_protocol()} for both plots.
Default \code{FALSE}.}

\item{baseline_heat_dt}{Numeric scalar. Bin width (s) for baseline heat strip.
Default \code{0.5}.}

\item{wash_heat_dt}{Numeric scalar. Bin width (s) for wash heat strip.
Default \code{1}.}
}
\value{
A \code{ggplot}/\code{patchwork} object containing the two plots side-by-side.
}
\description{
Builds two single-protocol ASP plots (baseline and wash) using
\code{plot_asp_single_protocol()} and combines them into a two-column layout
with consistent styling.
}
\details{
The function expects protocol keys to be stored in \code{x$dfs$dwin$baseline_key}
and \code{x$dfs$dwin$wash_key}. It also expects the ASP object at \code{x$dfs$asp}.
}
\examples{
\dontrun{
p <- plot_sidebyside_heat_wash(x = srt)
p

# Override defaults
p2 <- plot_sidebyside_heat_wash(
  x = srt,
  heat_dt = 1,
  heat_dt = 1,
  y_lim = c(-3, 2)
)
}

}
