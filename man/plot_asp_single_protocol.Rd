% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_asp_single_protocol.R
\name{plot_asp_single_protocol}
\alias{plot_asp_single_protocol}
\title{Plot a single protocol section from an ASP object}
\usage{
plot_asp_single_protocol(
  asp,
  protocol_key = NULL,
  key_match = NULL,
  bl_window = c(-5, 0),
  y_mode = c("percent", "log10_percent", "signed_log10_delta", "log2_fc"),
  y_lim = NULL,
  x_lim = NULL,
  log_eps_hz = 0.05,
  alpha_trace = 0.9,
  linewidth = 0.35,
  heat_dt = 0.1,
  ttl_shape = 6,
  heat_method = c("mean", "median"),
  show_sweep_lines = TRUE,
  sweep_line_alpha = 0.35,
  save_png = FALSE,
  png_path = "figs/exp_single_heat",
  save_to_rookery = TRUE,
  show_plot = TRUE,
  fig_w = 10,
  fig_h = 4.5,
  dpi = 300,
  verbose = TRUE
)
}
\arguments{
\item{asp}{A nested list returned by \code{build_asp()}. Must contain \code{asp$by_protocol}
(named list) and \code{asp$md$cell_name}. Each entry in \code{asp$by_protocol[[key]]} must
include \code{spike_puff_output} with at least \code{time} and \code{instRate}, and optionally
\code{percent_change}, \code{blMean}, and \code{segment_bounds}.}

\item{protocol_key}{Character. Exact name of the protocol entry to plot (must match
one of \code{names(asp$by_protocol)}). If provided, takes precedence over \code{key_match}.}

\item{key_match}{Character. Regular expression used to match against
\code{names(asp$by_protocol)}. If multiple keys match, the first is used (with a message
when \code{verbose = TRUE}).}

\item{bl_window}{Numeric length-2 vector giving the baseline time window in seconds
(e.g. \code{c(-5, 0)}). Used only if \code{percent_change} is missing and/or \code{obj$blMean} is
unavailable/invalid.}

\item{y_lim}{Optional numeric length-2 vector. If provided, sets the visible y-range
via \code{coord_cartesian(ylim = y_lim)} (x-limits remain the full local time span).}

\item{log_eps_hz}{Numeric. Small constant added to \code{instRate} and baseline when
\code{y_mode = "log2_fc"} to avoid \code{-Inf} when firing rate approaches 0.}

\item{alpha_trace}{Numeric in \link{0, 1}. Alpha for the colored points in the trace panel.}

\item{linewidth}{Numeric. Line width used for the black trace line in the trace panel.}

\item{heat_dt}{Numeric. Bin width (seconds) for the heat strip. InstRate is aggregated
within bins and then filled to make a continuous strip.}

\item{ttl_shape}{Integer. Point shape used for the green TTL down-arrow marker
in the heat panel (default \code{6}).}

\item{heat_method}{Character. Aggregation function for instRate within each heat bin:
\code{"mean"} or \code{"median"}.}

\item{show_sweep_lines}{Logical. If \code{TRUE} and \code{segment_bounds} is present, draw dotted
vertical lines at sweep/segment boundaries (\code{segment_bounds$start_time}).}

\item{sweep_line_alpha}{Numeric in \link{0, 1}. Alpha for the dotted sweep boundary lines.}

\item{save_png}{Logical. If \code{TRUE}, save the combined plot to disk via \code{ggsave()}.}

\item{png_path}{Character. Output path for the PNG. If \code{NULL} and \code{save_png = TRUE},
defaults to \code{file.path(getwd(), paste0(asp$md$cell_name, "-asp-spike-protocol.png"))}.}

\item{fig_w}{Numeric. Width (in inches) passed to \code{ggsave()}.}

\item{fig_h}{Numeric. Height (in inches) passed to \code{ggsave()}.}

\item{dpi}{Integer. DPI passed to \code{ggsave()}.}

\item{verbose}{Logical. If \code{TRUE}, print messages about key selection and TTL placement.}
}
\value{
Invisibly returns a list with:
\describe{
\item{\code{protocol_key}}{The protocol key that was plotted.}
\item{\code{df_trace}}{Filtered/augmented trace data used for plotting (includes \code{y_plot}).}
\item{\code{df_heat}}{Binned heat-strip data (time bins and instRate).}
\item{\code{ttl_times_local}}{The chosen TTL time (closest sampled time to 0).}
\item{\code{plot}}{The combined patchwork plot object.}
}
}
\description{
Builds a two-panel plot for \strong{one} protocol entry inside \code{asp$by_protocol}:
(1) a trace of the response over time (with points colored by instantaneous firing rate),
and (2) a one-row heat strip (binned by \code{heat_dt}) showing instRate over time with a
green down-arrow at the TTL onset.
}
\details{
This function assumes the selected protocol's \code{spike_puff_output$time} is already
in \strong{local time} with TTL onset near \strong{0 s} (i.e., do \emph{not} use global/concatenated
segment start times to place the TTL marker).

If \code{percent_change} is missing, it is computed as \code{(instRate / baseline) * 100},
using \code{obj$blMean} when valid, otherwise recomputed from \code{bl_window}.
For TTL placement in this single-protocol mode, the marker is placed at the time
value in \code{spike_puff_output$time} closest to 0 seconds.
}
\examples{
\dontrun{
# Plot by exact key
plot_asp_single_protocol(asp, protocol_key = names(asp$by_protocol)[1])

# Plot by regex match
plot_asp_single_protocol(asp, key_match = "Standard_Puff", y_mode = "log2_fc")

# Set y-limits and save
plot_asp_single_protocol(
  asp,
  key_match = "wash-in",
  y_mode = "percent",
  y_lim = c(-1, 1),
  save_png = TRUE
)
}
}
