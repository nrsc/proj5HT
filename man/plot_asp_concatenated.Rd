% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_asp_concat.R
\name{plot_asp_concatenated}
\alias{plot_asp_concatenated}
\title{Plot concatenated ASP protocols with a trace + heat strip}
\usage{
plot_asp_concatenated(
  asp,
  gap_s = 2,
  order_by = c("wrapped_sweeps", "protocol_key"),
  bl_window = c(-5, 0),
  y_mode = c("percent", "log10_percent", "signed_log10_delta", "log2_fc"),
  y_arrow_pad = NULL,
  y_lim = NULL,
  x_lim = NULL,
  x_lim_by_protocol = NULL,
  alpha_trace = 0.9,
  linewidth = 0.35,
  show_gap_lines = TRUE,
  gap_line_alpha = 0.35,
  min_rate_hz = 0.05,
  heat_dt = 0.1,
  heat_method = c("mean", "median"),
  save_png = FALSE,
  rookery_path = "rookery",
  png_path = NULL,
  png_w = 14,
  png_h = 5,
  dpi = 300,
  verbose = TRUE
)
}
\arguments{
\item{asp}{A nested list returned by \code{build_asp()}. Must contain \code{asp$by_protocol}
(named list) and \code{asp$md$cell_name}. Each \code{asp$by_protocol[[key]]} should include
\code{spike_puff_output} with columns \code{time} and \code{instRate}, and optionally
\code{percent_change}, \code{blMean}, \code{segment_bounds}, and \code{pro_row} metadata used for
ordering/labels (e.g. \code{wrapped_sweeps}, \code{stimulus_description}, \code{Exp},
\code{assigned_subclass}).}

\item{gap_s}{Numeric. Gap duration (seconds) inserted between consecutive protocol
segments in global time.}

\item{order_by}{Character. Ordering rule for protocols:
\describe{
\item{\code{"wrapped_sweeps"}}{Order by numeric sweep ids parsed from \code{pro_row$wrapped_sweeps}
(min sweep, then max sweep, then type/key).}
\item{\code{"protocol_key"}}{Order by protocol type then key.}
}}

\item{bl_window}{Numeric length-2 vector. Baseline window in seconds (e.g. \code{c(-5,0)}),
used when \code{percent_change} is missing and/or \code{blMean} is invalid.}

\item{y_arrow_pad}{Optional numeric. Vertical padding added above each protocol’s
segment maximum (in \code{y_plot} units) to place TTL markers. If \code{NULL}, a default
is chosen based on \code{y_mode}.}

\item{y_lim}{Numeric length-2 vector. Visible y-range for the trace panel via
\code{coord_cartesian(ylim = y_lim)}.}

\item{alpha_trace}{Numeric in \link{0,1}. Alpha for trace points in the main panel.}

\item{linewidth}{Numeric. Line width for the faint black backbone trace.}

\item{show_gap_lines}{Logical. If \code{TRUE}, draw dotted vertical lines at each
protocol segment start (\code{df_starts$x0}) in both panels.}

\item{gap_line_alpha}{Numeric in \link{0,1}. Alpha for the dotted gap/protocol-start lines.}

\item{min_rate_hz}{Numeric. Minimum firing rate (Hz) used as a floor when
\code{y_mode = "log2_fc"} to prevent \code{-Inf} and preserve the trace shape through
silent periods.}

\item{heat_dt}{Numeric. Bin width (seconds) for the heat strip. InstRate is
aggregated within bins and filled to create a continuous strip.}

\item{heat_method}{Character. Aggregation for instRate within each bin: \code{"mean"} or
\code{"median"}.}

\item{save_png}{Logical. If \code{TRUE}, save the combined plot to disk via \code{ggsave()}.}

\item{rookery_path}{Character. Base directory used when constructing a default
\code{png_path} (typically a per-cell folder under \code{"rookery"}).}

\item{png_path}{Character. Output file path for the PNG. If \code{NULL} and
\code{save_png = TRUE}, defaults to
\code{file.path(rookery_path, asp$md$cell_name, paste0(asp$md$cell_name, "-asp-concat-heat.png"))}.
If a directory path is provided (no \code{.png} suffix), the file name is appended.}

\item{png_w}{Numeric. Width (in inches) passed to \code{ggsave()}.}

\item{png_h}{Numeric. Height (in inches) passed to \code{ggsave()}.}

\item{dpi}{Integer. DPI passed to \code{ggsave()}.}

\item{verbose}{Logical. If \code{TRUE}, emit per-protocol TTL diagnostics and a TTL
sanity check summary.}
}
\value{
Invisibly returns a list with:
\describe{
\item{\code{df_concat}}{Concatenated trace data for all plotted protocols, including
\code{time_global}, \code{protocol_key}, \code{protocol_index}, and \code{y_plot}.}
\item{\code{df_arrows}}{TTL marker table in global time with per-protocol y placement.}
\item{\code{df_starts}}{Protocol segment start/end table (\code{x0}, \code{x1}) in global time.}
\item{\code{df_heat}}{Heat-strip binned data (global time bins and instRate). Bins
outside any protocol segment are set to \code{NA}.}
\item{\code{ttl_check}}{Per-protocol TTL sanity check summary (source, counts, range check).}
\item{\code{plot}}{The combined patchwork plot object.}
}
}
\description{
Concatenates all protocol entries in \code{asp$by_protocol} into one continuous
\strong{global time} axis separated by a fixed gap (\code{gap_s}). Produces a two-panel
plot: (1) the response trace (points colored by \code{instRate}) and (2) a one-row
heat strip of \code{instRate} binned by \code{heat_dt}, with green TTL markers placed
at each protocol’s TTL onset (converted into global time).
}
\details{
Protocols can be ordered either by numeric sweep indices parsed from
\code{pro_row$wrapped_sweeps} or by \code{protocol_key}. Gaps between protocols may be
indicated by dotted vertical lines at each protocol start.

\strong{Global time construction:} each protocol’s local time is shifted by \code{seg_start}
such that the segment begins at the current \code{offset}. The offset is then advanced
to the end of the segment plus \code{gap_s}.

\strong{Percent change:} if \code{percent_change} is absent, it is computed as
\code{(instRate / baseline) * 100} using \code{obj$blMean} when valid, otherwise from
\code{bl_window}.

\strong{TTL placement:} TTL times are sourced in this priority order:
\enumerate{
\item \code{segment_bounds$ttl_onset_time} for TTL-related chunks when present and finite
\item \code{segment_bounds$start_time + obj$ttlTime} as a fallback when onset times are absent
\item local \code{t = 0} when no usable segment bounds are available
}
TTL times are then snapped to the nearest available sample time in the local trace
and shifted into global time for plotting.

\strong{Heat strip:} bins are computed across the full global span, filled in both
directions, and then blanked (\code{NA}) outside any protocol segment.
}
\examples{
\dontrun{
# Basic concatenated view ordered by sweeps
plot_asp_concatenated(asp, order_by = "wrapped_sweeps", y_mode = "log2_fc")

# Tight y-range, larger gaps, save to rookery
plot_asp_concatenated(
  asp,
  gap_s = 3,
  y_mode = "signed_log10_delta",
  y_lim = c(-1.5, 1.5),
  save_png = TRUE
)

# Use median heat bins and hide gap lines
plot_asp_concatenated(
  asp,
  heat_method = "median",
  show_gap_lines = FALSE
)

order_by = "wrapped_sweeps"
y_mode = "log2_fc"
bl_window = c(-5, 0)
y_arrow_pad = NULL
y_lim = c(-1,1)
alpha_trace = 0.9
linewidth = 0.35
show_gap_lines = TRUE
min_rate_hz = 0.05
gap_line_alpha = 0.35
heat_dt = 0.10
heat_method = c("mean", "median")
verbose = TRUE

}
}
