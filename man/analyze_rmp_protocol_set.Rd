% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze_rmp_protocol_set.R
\name{analyze_rmp_protocol_set}
\alias{analyze_rmp_protocol_set}
\title{Analyze an RMP protocol set (chunk + pulse-train summaries)}
\usage{
analyze_rmp_protocol_set(
  tst,
  on_s = 0.1,
  off_s = 0.9,
  early_off_s = 0.1,
  complete_frac = 0.8,
  downsample_for_plot = TRUE,
  dvdt_thresh_mV_ms = 5,
  slow_bin_ms = 10,
  fast_keep_every = 1,
  max_points = 50000,
  thin_slow_every = 2,
  make_plots = TRUE,
  return_level = c("light", "full")
)
}
\arguments{
\item{tst}{Data frame with at least columns: \code{time}, \code{tpRMP}, \code{chunk}.
If \code{pulse_state} exists, pulse-train analysis is performed within \code{chunk == "tpRMP_puff"}.}

\item{on_s}{Expected pulse ON duration (seconds).}

\item{off_s}{Expected pulse OFF duration (seconds).}

\item{early_off_s}{Early window (seconds) within OFF to compute early peak.}

\item{complete_frac}{Fraction of expected duration required for last segment to be considered complete.}

\item{downsample_for_plot}{Logical; downsample returned plot dataframe.}

\item{dvdt_thresh_mV_ms}{Threshold for fast segments in downsampling (if you use adaptive downsample).}

\item{slow_bin_ms}{Bin size for slow segments in downsampling.}

\item{fast_keep_every}{Keep every N points in fast segments.}

\item{max_points}{Cap on returned plot points.}

\item{thin_slow_every}{Thin factor for slow segments.}

\item{make_plots}{Logical; if TRUE and full return, includes ggplot trace.}

\item{return_level}{\code{"light"} or \code{"full"}.}
}
\value{
A list with:
\describe{
\item{chunk_summary}{tibble of per-chunk summary stats}
\item{bl_mean}{baseline mean (tpRMPbl/tpRMP_bl) or NA}
\item{trail_mean}{trail mean or NA}
\item{pulse}{NULL or list of pulse summaries}
\item{plot_trace_df}{(full) downsampled df for trace plot}
\item{plot_trace}{(full) ggplot trace}
}
}
\description{
Computes:
\itemize{
\item Per-chunk summary stats over aligned time (\code{tpRMPbl}, \code{tpRMP_puff}, \code{trail})
\item Optional pulse-train summaries within the \code{tpRMP_puff} chunk if \code{pulse_state}
is present (\code{pulse_on} and \code{pulse_off} segment features).
\item Optional plotting artifacts when \code{return_level = "full"}.
}
}
