% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/child_payload5HT.R
\name{nnest5HT}
\alias{nnest5HT}
\title{Create / update the proj5HT child payload nnest for a cell (srt)}
\usage{
nnest5HT(
  x,
  tag_child = "-srt.rds",
  save_nnest = TRUE,
  update = TRUE,
  write = TRUE,
  verbose = TRUE,
  payload_keys = c("dfs", "ana", "smry", "params", "results", "log"),
  mimic_header = TRUE
)
}
\arguments{
\item{x}{cell name (character) OR an HCT nnest/list with $cell}

\item{tag_child}{filename tag for child payload (default "-srt.rds")}

\item{save_nnest}{write payload to disk}

\item{update}{if TRUE, preserves existing payload (dfs/ana/etc.) when rebuilding}

\item{write}{if FALSE, dry run (no file writes)}

\item{verbose}{messages}

\item{payload_keys}{which top-level keys to carry forward from an existing payload on update}

\item{mimic_header}{if TRUE, attach header-like fields (cell/wd/rd/md/files) onto returned child}
}
\value{
child nnest object (list). Status info is attached as attr(child, "status")
}
\description{
HCT (-hct.rds) is the authoritative header (static state).
proj5HT child (-srt.rds) is mutable analysis payload (dfs/ana/etc.).
}
