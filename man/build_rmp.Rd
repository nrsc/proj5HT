% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_rmp.R
\name{build_rmp}
\alias{build_rmp}
\title{Build RMP protocol structures from tpRMP sweeps}
\usage{
build_rmp(
  x,
  mMD = NULL,
  mMD_path = "~/proj5HT/den/serotonin/Data/Master_UI_data.ods",
  mMD_sheet = "Master_Macaque",
  puff_default = 5,
  outlier_sd = 5,
  bl_window = c(-5, 0),
  save_srt = TRUE,
  write_csv = FALSE,
  store_level = c("light", "full"),
  csv_tag = "-rmp.csv"
)
}
\arguments{
\item{x}{A \code{srt} object (list) or a character cell ID (will be loaded via \code{load5HT()}).}

\item{mMD}{Optional Master sheet data.frame; if NULL it is read from \code{mMD_path}.}

\item{mMD_path}{Path to Master_UI_data.ods}

\item{mMD_sheet}{Sheet name inside the ODS}

\item{puff_default}{Default puff time if TTLtime is missing.}

\item{outlier_sd}{Outlier filter threshold in SDs (tpRMP).}

\item{bl_window}{Baseline window on aligned time axis, default c(-5, 0).}

\item{save_srt}{Save updated srt back to disk.}

\item{write_csv}{Write aligned time-series CSV per protocol.}

\item{store_level}{"light" or "full" (controls plot artifact retention).}

\item{csv_tag}{Suffix for CSV outputs.}
}
\value{
The \code{rmp} container (\code{srt$dfs$rmp}).
}
\description{
Drop-in analogue of \code{build_asp()}, but for tpRMP (membrane potential).
}
\details{
Key behaviors:
\itemize{
\item selects protocol rows where stimulus_description contains "tpRMP_puff"
\item chunk mapping uses "tpRMPbl" as baseline and preserves "trail"
\item stores outputs in \code{srt$dfs$rmp}
}
}
