% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hier_metadata5HT.R
\name{add_hier_to_seurat}
\alias{add_hier_to_seurat}
\title{Overlay hierarchical-classifier metadata onto a Seurat object.}
\usage{
add_hier_to_seurat(obj, cols = NULL)
}
\arguments{
\item{obj}{Seurat object whose \verb{@meta.data} contains \code{cell_name}.}

\item{cols}{character vector of hier columns to attach (see \code{hier5HT()}).}
}
\value{
the same object with hier columns appended / normalized in
\verb{@meta.data}.
}
\description{
Convenience wrapper for objects not built via
\code{dev/Seurat/Seurat_Objects.R::build_seurat_from_feather()} (which already
joins \code{cleanMD} and normalizes in-place). Left-joins \code{hier5HT()} to
\code{obj@meta.data} on \code{cell_name}; any hier columns already present are
re-normalized via \code{normalize_hier_assignments()} instead of being
duplicated.
}
