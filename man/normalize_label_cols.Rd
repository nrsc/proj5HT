% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hier_metadata5HT.R
\name{normalize_label_cols}
\alias{normalize_label_cols}
\title{Per-cell hierarchical-classifier metadata.}
\usage{
normalize_label_cols(df, cols)
}
\arguments{
\item{df}{data.frame (or any 2D structure accepting \code{df[[col]] <- ...}).}

\item{cols}{character vector of column names to normalize.}
}
\value{
data.frame with at least \code{cell_name} plus the requested hier columns.
Normalize categorical label columns to the proj5HT convention.

Core string normalizer: replaces spaces with underscores
(e.g. \code{"L2/3 IT"} → \code{"L2/3_IT"}) on any column whose name is in \code{cols}
and is present in \code{df}. Empty strings and \code{NA}s pass through unchanged.
Idempotent.

This is the single source of truth used by both \code{normalize_hier_assignments()}
(headBCI hier classifier output) and \code{normalize_taxonomy_labels()} (AIBS
10x / PatchSeq taxonomy \verb{*_label} columns).

\code{df} with the named columns normalized in place.
}
\description{
Pulls hier_* columns from \code{headBCI::sheets$cleanMD}, normalizes
subclass/cluster strings to the proj5HT convention (spaces replaced with
underscores), and returns one row per \code{cell_name}.
}
