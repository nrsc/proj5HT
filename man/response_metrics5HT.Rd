% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/response_metrics5HT.R
\name{response_metrics5HT}
\alias{response_metrics5HT}
\title{Robust response metrics for a single cell's puff trace}
\usage{
response_metrics5HT(
  df_cell,
  baseline_window = c(-5, 0),
  response_window = c(0, 10),
  cusum_k = 0.5,
  cusum_h = 5,
  wasserstein_grid = 1024
)
}
\arguments{
\item{df_cell}{data.frame for a single cell with at least the columns
\code{time} (seconds, TTL-aligned) and \code{instRate} (Hz).}

\item{baseline_window}{numeric length-2.  Pre-puff window (s) used to
estimate baseline mean, SD, and drift.}

\item{response_window}{numeric length-2.  Post-puff window (s) over
which the response metrics are computed.}

\item{cusum_k}{slack in baseline-SD units passed to the CUSUM detector.}

\item{cusum_h}{decision threshold in baseline-SD units for CUSUM.}

\item{wasserstein_grid}{quantile-grid resolution for the 1-D
Wasserstein computation.}
}
\value{
a one-row \code{tibble} with the metric panel described in
the file header (baseline diagnostics + z / Cohen's d / detrended
percent / CUSUM onset / ΔCV / ΔFano / KS / Wasserstein).
}
\description{
Computes a panel of complementary response metrics that are robust to
bursty or drifting baselines.  Operates on a single cell's rows from
the canonical long-format puff dataframe (see \code{prep_puff_df()}).
}
\details{
All time values are expected to be aligned so that TTL onset is at
\code{time = 0}.
}
\examples{
\dontrun{
  srt <- load5HT("H23.30.001.01")
  df  <- srt$dfs$asp$by_protocol[[1]]$spike_puff_output
  response_metrics5HT(df)
}
}
