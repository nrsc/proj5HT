% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify_response5HT.R
\name{classify_response5HT}
\alias{classify_response5HT}
\title{Classify 5-HT puff responses into 4 categories}
\usage{
classify_response5HT(
  df,
  rapid_window = c(0, 10),
  extended_window = c(10, 50),
  thr_pct = 15,
  min_persist_s = 1,
  smooth_k = 5,
  zero_thr_pct = 5,
  min_zero_s = 0.5,
  carry_cols = NULL
)
}
\arguments{
\item{df}{Long-format dataframe. Must contain \code{cell_name}, \code{time},
\code{percent_change}. Optional grouping columns (e.g.
\code{assigned_subclass}, \code{Species}) are carried through.}

\item{rapid_window}{numeric length-2, default \code{c(0, 10)}.}

\item{extended_window}{numeric length-2, default \code{c(10, 50)}.}

\item{thr_pct}{excursion threshold (percent units), default \code{15}.}

\item{min_persist_s}{minimum contiguous duration (s), default \code{1}.}

\item{smooth_k}{odd integer rolling-mean window, default \code{5}.}

\item{zero_thr_pct}{\code{percent_change} below this is treated as silent
(default \code{5}).}

\item{min_zero_s}{minimum contiguous silent duration (s) to flag
inhibition (default \code{0.5}).}

\item{carry_cols}{columns to keep on the per-cell output. Auto-
detected from \code{df} if \code{NULL}.}
}
\value{
tibble with one row per cell containing:
\code{cell_name}, optional carried columns, \code{response_type} (factor),
\code{rapid_exc}, \code{rapid_inh}, \code{ext_exc}, \code{ext_inh} (logical persistence
flags), \code{exc_peak_time}, \code{inh_peak_time}, \code{exc_peak_pct},
\code{inh_peak_pct}.
}
\description{
A focused, self-contained classifier that uses both a "rapid" early
window and an "extended" later window to call each cell as one of:
}
\details{
\itemize{
\item \code{"excitation"}        — sustained excursion above baseline only
\item \code{"inhibition"}        — sustained excursion below baseline only
\item \code{"biphasic_exc_inh"}  — both directions, excitation comes first
\item \code{"biphasic_inh_exc"}  — both directions, inhibition comes first
\item \code{"no_change"}         — no sustained excursion meeting threshold
}

Operates directly on a long-format dataframe (one row per cell × time)
with at minimum \code{cell_name}, \code{time}, \code{percent_change}. This is the same
format returned by \code{figure_df_build()}, so the classifier can be applied
to any of those dfs without a separate prep step.

A "sustained excursion" is a contiguous run of smoothed
\code{percent_change} that crosses \verb{100 ± thr_pct} for at least
\code{min_persist_s} seconds inside the given window.

In addition to the smoothed test, an unsmoothed \strong{zero-spiking} check
is run: any contiguous run of raw \code{percent_change <= zero_thr_pct} (i.e.
the cell stopped firing) lasting at least \code{min_zero_s} seconds inside
a window forces the corresponding \verb{*_inh} flag to TRUE. This guarantees
that a brief but unambiguous silent period is never missed by the
smoother, so an otherwise excitatory cell with a transient silence at
puff onset is correctly classified as biphasic.

The biphasic order is decided by the order of the two extrema (max for
excitation, min for inhibition) inside the full \code{combined_window}. The
rapid window matters because it gives a stricter detection bar for
fast responses, but a slow inhibitory rebound in the extended window
still counts toward biphasic.
}
\seealso{
\link{classify5HT_helpers.R} for the \code{smooth_vec},
\code{has_persistent_excursion}, and \code{get_extreme_in_window} helpers reused
here.
}
